{
	"name": "checkr",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"cf-typegen": "wrangler types -c wrangler.jsonc -c ../../wrangler.jsonc",
		"type-check": "npm run cf-typegen && tsc"
	},
	"devDependencies": {
		"@types/node": "^22.20.2",
		"typescript": "^5.9.3",
		"wrangler": "^4.130.0"
	}
}
